# Production Environment Variables Example
# Copy this file to .env.production for production builds

# Application Information
VITE_APP_NAME=TimeHogger
VITE_APP_VERSION=0.0.2
VITE_APP_DESCRIPTION=Personal time tracking application

# Production Settings
VITE_NODE_ENV=production
VITE_DEV_TOOLS=false

# API Configuration for Production
# Option 1: Use specific API server URL
# VITE_API_URL=https://api.yourdomain.com/api

# Option 2: Use same domain as frontend (recommended)
# VITE_API_URL=/api

# Option 3: Use subdomain
# VITE_API_URL=https://api.yourdomain.com/api

# The API service will automatically detect the environment
# and use appropriate defaults if these are not set
